# requirements/yolo.txt

# --- Core ML/DL with specific CUDA for PyTorch ---
torch==2.0.1 --index-url https://download.pytorch.org/whl/cu118
torchvision==0.15.2 --index-url https://download.pytorch.org/whl/cu118
# torchaudio==2.0.2 --index-url https://download.pytorch.org/whl/cu118 # Add if yolov8 or its utils need it

ultralytics # For YOLOv8 models

# --- FastAPI and Pydantic (Updated for Pydantic v2 compatibility) ---
fastapi==0.104.1 # Matches main requirements.txt
pydantic==2.5.0    # Matches main requirements.txt

# --- Other necessary packages ---
uvicorn[standard]          # ASGI server
opencv-python-headless   # For image/video processing on a server
supervision              # If used for annotations in yolov8_service.py
httpx                    # If yolov8_service.py makes outbound HTTP calls
python-multipart         # For FastAPI file uploads
google-cloud-storage     # If yolov8_service.py interacts with GCS
numpy                    # Often a core dependency for CV/ML
