# YOLO-NAS Service Requirements
# Verified versions - super-gradients is still maintained open source
# GCS removed to avoid protobuf conflicts (see DOCKER_DEPENDENCY_FIXES.md)

# --- Core Dependencies (Compatible with super-gradients) ---
numpy>=1.21.0,<=1.23.5
# protobuf removed - let super-gradients manage its own version

# --- YOLO-NAS ---
super-gradients>=3.7.0

# --- Computer Vision ---
opencv-python-headless==4.10.0.84
pillow==10.4.0

# --- FastAPI Stack ---
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# --- Additional Dependencies ---
httpx==0.27.0

# --- Cloud & Storage ---
# google-cloud-storage removed - use gsutil CLI instead to avoid protobuf conflicts
# gsutil installed via Google Cloud SDK in Dockerfile - no Python dependency conflicts