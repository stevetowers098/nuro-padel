# YOLO-NAS VM Deployment Requirements
# Matches Dockerfile configuration for consistency

# --- Core ML Dependencies (must match Dockerfile) ---
numpy==1.23.0  # Super-gradients constraint
torch  # Let super-gradients manage PyTorch version for cu118
torchvision
torchaudio

# --- Super-gradients (matches Dockerfile) ---
super-gradients==3.7.1

# --- Core Dependencies ---
requests==2.31.0

# --- Computer Vision ---
opencv-python-headless==4.10.0.84
pillow==10.4.0

# --- FastAPI Stack ---
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# --- Additional Dependencies ---
httpx==0.27.0

# Note: Documentation tools (sphinx, docutils) removed for leaner runtime
# Note: Google Cloud SDK (gsutil) should be installed separately for GCS uploads