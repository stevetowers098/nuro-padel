# YOLO-NAS Service Requirements
# super-gradients installed first in Dockerfile - manages PyTorch, numpy, protobuf versions
# gsutil CLI handles GCS uploads - no Python dependency conflicts

# --- Computer Vision ---
opencv-python-headless==4.10.0.84
pillow==10.4.0

# --- FastAPI Stack ---
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# --- Additional Dependencies ---
httpx==0.27.0

# Core ML dependencies (PyTorch, numpy, protobuf) managed by super-gradients
# GCS uploads handled by gsutil CLI (Google Cloud SDK)