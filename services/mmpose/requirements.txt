# MMPose Service Requirements
# Verified versions for MMPose stack - Updated with pinned versions for compatibility

# --- Core ML/DL with CUDA Support ---
torch==2.1.2 --index-url https://download.pytorch.org/whl/cu121
torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu121
torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cu121

# --- MMPose Stack ---
mmcv==2.1.0

# --- Core Dependencies ---
numpy>=1.24.0,<2.0.0
cython>=3.0.0

# --- COCO Tools ---
pycocotools>=2.0.8
xtcocotools>=1.14

# --- MMPose Stack (install via mim in Dockerfile) ---
openmim>=0.3.0
mmengine==0.9.0
mmpose==1.0.0

# --- Computer Vision ---
opencv-python-headless==4.10.0.84
pillow==10.4.0

# --- FastAPI Stack ---
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# --- Additional Dependencies ---
httpx==0.27.0
pytz==2023.3 # Pinned for OpenXLab compatibility

# --- Cloud & Storage ---
google-cloud-storage==2.10.0 # Pinned to avoid protobuf conflicts