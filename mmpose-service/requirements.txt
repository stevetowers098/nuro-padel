# MMPose Service Requirements
# Compatible dependencies installed first in Dockerfile (pytz==2023.3, numpy<2.0)

# --- Computer Vision ---
opencv-python-headless==4.10.0.84
pillow==10.4.0

# --- FastAPI Stack ---
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# --- Additional Dependencies ---
httpx==0.27.0

# --- Cloud & Storage ---
google-cloud-storage==2.18.0

# Core dependencies (pytz, numpy) installed first in Dockerfile
# MMPose and mmcv installed via MIM in Dockerfile