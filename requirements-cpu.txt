# CPU-Only Requirements for Space-Constrained Deployments
# Use with: pip install --no-cache-dir -r requirements-cpu.txt

# === Core ML Stack (CPU-Only) ===
torch==2.3.1+cpu --index-url https://download.pytorch.org/whl/cpu
torchvision==0.18.1+cpu --index-url https://download.pytorch.org/whl/cpu

# === Computer Vision ===
opencv-python-headless==4.10.0.84
pillow==10.4.0
numpy>=1.24.4

# === FastAPI Stack ===
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# === Additional Dependencies ===
httpx==0.27.0
requests>=2.31.0

# === TrackNet Dependencies ===
matplotlib==3.8.2
scipy==1.10.1
tqdm==4.66.1

# === Cloud Storage ===
google-cloud-storage==2.10.0

# Note: This configuration saves ~1.5GB per service compared to GPU versions
# Performance: CPU inference is 3-5x slower but sufficient for development/testing