# YOLO-NAS Service Requirements
# super-gradients==3.7.1 installed first in Dockerfile

# --- Globally compatible versions for all packages ---
numpy==1.24.4
sphinx>=6,<9
requests==2.31.0
docutils>=0.18,<0.22

# --- Computer Vision ---
opencv-python-headless==4.10.0.84
pillow==10.4.0

# --- FastAPI Stack ---
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# --- Additional Dependencies ---
httpx==0.27.0

# super-gradients==3.7.1 handles PyTorch and its own dependencies
# GCS uploads handled by gsutil CLI (Google Cloud SDK)