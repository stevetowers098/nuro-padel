# YOLO-NAS Service Requirements
# super-gradients==3.7.1 installed first in Dockerfile
# Override incompatible dependency versions that super-gradients installs

# --- Core Dependencies (Compatible with super-gradients==3.7.1 constraints) ---
requests>=2.31.0               # pyhanko packages require >=2.31.0, super-gradients installs 2.22.0
numpy<=1.23                    # super-gradients requires numpy<=1.23
docutils<0.19                  # super-gradients requires docutils<0.19
sphinx>=6,<9                   # sphinx-rtd-theme requires >=6,<9, super-gradients installs 4.0.3

# --- Computer Vision ---
opencv-python-headless==4.10.0.84
pillow==10.4.0

# --- FastAPI Stack ---
fastapi==0.111.0
pydantic==2.7.4
uvicorn[standard]==0.30.1

# --- Additional Dependencies ---
httpx==0.27.0

# super-gradients==3.7.1 handles PyTorch, but installs incompatible secondary packages
# GCS uploads handled by gsutil CLI (Google Cloud SDK)